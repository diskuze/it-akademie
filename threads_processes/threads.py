import random
import threading

def main():
    """
    TODO:
    - V této funkci si zkuste vyrobit 2 vlákna a pomocí nich vygenerovat
      100 000 000 náhodných čísel tak, že každé vlákno bude generovat právě polovinu z nich.

    - Pomocí systémových nástrojů si zkuste stopnout čas běhu tohoto programu.
    """

    # t1 = threading.Thread(target=..., args=(...))
    pass

if __name__ == "__main__":
    main()
